# 服务端改进项记录

本文档用于记录服务端和同进程管理员端后续需要讨论、确认和实现的问题。

## 使用流程

1. 讨论阶段：只分析问题、影响范围、可选方案和实现风险，不修改服务端代码。
2. 记录阶段：当负责人明确说“记录这个修改项”时，将已经确认的内容追加到本文档。
3. 实现阶段：只有负责人明确说“根据 improvement.md 开始修改”后，才统一处理本文档中的待实现项。
4. 验证阶段：实现后补充测试结果、相关提交或 PR，并将已完成项标记为 `DONE`。
5. 版本阶段：正式完成的改动同步追加到 [`版本日志.md`](版本日志.md)；如果涉及客户端联调的接口，再同步更新 [`服务端网络接口.md`](服务端网络接口.md)。

## 状态说明

| 状态 | 含义 |
| --- | --- |
| `DISCUSSING` | 正在讨论，尚未确认记录 |
| `TODO` | 已确认，等待统一实现 |
| `BLOCKED` | 受到外部条件阻塞，需要补充信息或其他模块配合 |
| `DONE` | 已实现并完成验证 |
| `WONT_DO` | 讨论后决定暂不实现 |

## 当前记录

## v2.0 修改审计说明

本节用于补齐从上一版 GitHub 基线到当前本地代码的已落地修改，避免只记录“单个问题”而遗漏在验收过程中直接确认的细节。

- 审计基线：GitHub PR #12 合并提交 `044a728`（`feat(server): 管理端 v1.2 站点与电桩管理优化`，分支 `server/admin-v1.2`）。
- 对比范围：`044a728` 之后本地 `server/` 服务端及同进程 Qt 管理员端的代码、测试和文档修改。
- 远端核对：当前 `origin/main` 为 `a8b5250`（PR #14，客户端腾讯地图导航）；`044a728..origin/main` 没有新的服务端提交，因此客户端地图、Web 大屏和客户端 TCP 变更不纳入本服务端改进记录。
- 记录口径：用户明确要求并已实现的修改标记为 `DONE`；同一问题的中间方案由后续最终方案覆盖，并在原记录中注明，不把中间状态误写成最终行为。
- 数据边界：本轮没有新增数据库表、字段、迁移或客户端 TCP 消息；管理员端仍通过 `AdminFacade -> ApplicationService -> IRepository` 完成管理操作。

## IMP-001：精简管理员登录界面文案和提示区域

- 状态：DONE
- 记录日期：2026-09-03
- 提出背景：管理员服务端窗口当前显示运行模式、业务功能介绍、登录说明和课程 Demo 账号等辅助文案。这是服务端固定运行界面，不需要向管理员展示数据源、课程演示信息或宣传性说明，现有小字较多且容易造成界面干扰。
- 目标结果：精简管理员登录界面和管理后台中的非必要提示，只保留必要的品牌标题、账号输入框、密码输入框、登录按钮以及实际业务内容和必要的服务状态。
- 影响范围：服务端同进程管理员 UI。
- 接口/数据影响：无；不修改 SQLite Repository、数据库文件、TCP 接口、命令行参数或后端运行逻辑。
- 验收标准：管理员登录页和登录后的管理后台均不再出现“当前模式”“SQLite 本地数据库”“内存开发模式”“站点运营 · 设备监控 · 用户管理 · 营收分析”“登录后台，查看充电业务运行情况”“课程 Demo 账号：admin / 123456”及对应提示区域；服务端仍能正常使用 SQLite 数据库启动和读写数据。
- 讨论结论：管理员窗口只有一种对外呈现方式，不提供运行模式展示或切换入口；登录界面不展示课程 Demo 账号，不展示宣传性功能列表或登录说明；本项只删除 UI 中的非必要文案和提示区域，不删除后端保留的测试替身能力。
- 依赖与风险：无。

### 实现记录

- 开始实现：2026-09-03
- 完成实现：2026-09-03
- 代码提交或 PR：待填写
- 验证结果：登录页和后台技术提示已删除；服务端编译、测试及 SQLite 启动冒烟通过。

## IMP-002：优化管理员登录界面的文字层级与布局

- 状态：DONE
- 记录日期：2026-09-03
- 提出背景：当前管理员登录界面的标题、字段标签、输入区域和登录按钮在字号、间距、对齐方式及垂直位置上还需要进一步优化，整体视觉重心不够统一。
- 目标结果：建立清晰、舒适、专业的登录表单层级，让管理员打开页面后能快速识别标题、输入账号密码并完成登录；文字大小、间距和位置与 [`uidesign.md`](uidesign.md) 中的层级和简洁原则一致。
- 影响范围：服务端同进程管理员 UI 的登录页面布局和文字样式。
- 接口/数据影响：无；不修改登录接口、认证逻辑、SQLite Repository、数据库、命令行参数或后端运行逻辑。
- 验收标准：品牌标题、页面标题、字段标签、输入内容、错误反馈和登录按钮具有明确且不过度的字号层级；表单整体在登录卡片中视觉居中，账号、密码、错误提示和登录按钮保持自然垂直间距与一致对齐；窗口缩放后布局仍然稳定可用；不通过增加说明性小字填充空白或解释操作。
- 讨论结论：登录页优先保证信息层级、可读性和操作效率，不追求堆叠装饰或文字内容；具体字号、间距和布局方案在统一实现时根据现有界面和实际运行效果调整。
- 依赖与风险：无。

### 实现记录

- 开始实现：2026-09-03
- 完成实现：2026-09-03
- 代码提交或 PR：待填写
- 验证结果：登录页字号、字重、间距和控件密度已按全局规范调整；服务端编译及窗口初始化通过。

## IMP-003：营收统计自定义日期范围与文案清理

- 状态：DONE
- 记录日期：2026-09-03
- 提出背景：原数据概览中的营收折线图仅支持“近 7 日”和“近 30 日”，管理员无法查看任意业务日期范围；同时页面存在面向开发阶段的说明性小字和技术状态标识，不符合运营管理界面的简洁规范。
- 目标结果：为营收折线图增加自定义起止日期查询，并清理数据概览页中不必要的说明文案和开发实现信息，使页面聚焦于运营数据和管理操作。
- 影响范围：服务端同进程管理员 UI、营收统计查询参数及营收趋势聚合逻辑；不涉及客户端 TCP 接口。
- 接口/数据影响：管理员端内部 `ApplicationService::getDashboard`/`AdminFacade` 查询语义需要支持日期范围；继续读取现有订单支付时间和金额，不新增数据库表或字段，不改变客户端网络协议。
- 验收标准：
  - 右上角范围选择保留“近 7 日”“近 30 日”，并增加“自定义”；选择自定义后显示起始日期、终止日期和确认查询操作。
  - 起止日期按中国时区解释，起始日和终止日均包含；起始日期不得晚于终止日期，查询跨度限制为不超过 366 天；非法输入显示清晰的业务提示。
  - 自定义日期只影响营收折线图；“今日营收”“本月营收”“累计营收”三个指标卡保持各自固定业务含义。
  - 鼠标停留在折线图数据点附近时显示对应日期和具体营收金额，并高亮当前数据点。
  - 日期范围内没有订单的日期仍显示为 0，折线日期连续；图表标题“营收趋势（元）”和状态标题“电桩状态”等必要业务文案保留。
  - 删除“运营核心指标”和“状态由服务端数据源实时聚合。”等说明性小字。
  - 删除页面顶部的 TCP 地址/启动状态、SQLite/内存模式标识，以及侧边栏“SERVER ADMIN”等开发阶段技术信息；不在运营 UI 中暴露 TCP、SQLite、Repository、内存模式等实现概念。
- 讨论结论：负责人确认后，自定义范围仅作用于营收趋势图，默认使用中国时区并包含边界日期；指标卡不随图表范围改变；技术运行状态和实现说明移出面向运营人员的界面；本项已实现。
- 依赖与风险：无数据库结构依赖；需要同步更新仪表盘相关单元测试，确保既有 7/30 日行为和自定义日期校验均通过。

### 实现记录

- 开始实现：2026-09-03
- 完成实现：2026-09-03
- 代码提交或 PR：待填写
- 验证结果：7/30 日及自定义日期查询测试通过；技术文案已清理，窗口初始化通过。

## IMP-004：新增充电站时联动生成可编辑电桩明细

- 状态：DONE
- 记录日期：2026-09-04
- 提出背景：当前新增充电站只填写“初始电桩数”，服务端随后按数量自动生成编号、类型和功率。自动生成的电桩明细在创建前不可编辑，管理员无法在站点创建过程中录入真实设备信息，导致编号、充电类型和额定功率可能与实际部署不一致。
- 目标结果：在新增充电站页面中，电桩数量与电桩明细联动。管理员修改数量后，表单下方立即展示对应数量的电桩明细行，并可在提交前编辑每个电桩的编号、类型和额定功率；站点及其初始电桩作为一个完整业务对象保存。
- 影响范围：服务端管理员 UI 新增站点表单、管理员端进程内 `AdminFacade`/`ApplicationService`、SQLite Repository 与内存 Repository 的站点批量创建事务、相关测试和服务端接口文档；不涉及客户端 TCP 网络协议。
- 接口/数据影响：将管理员端创建站点入参从 `pileCount` 调整为可携带 `piles` 明细数组（为兼容过渡期可保留数量字段但以明细数组为准）；每项至少包含 `pileCode`、`pileType`（`FAST`/`SLOW`）和 `ratedPowerKw`。服务端继续生成 `stationId`、桩 ID、创建时间及初始状态 `IDLE`；不新增数据库表或字段，沿用现有 `charging_stations` 与 `charging_piles` 结构。
- 验收标准：
  - 新增站点页面的电桩数量范围为 0–100；数量为 0 时不展示明细并创建空站点。
  - 数量增加时追加默认明细行，数量减少时移除末尾未保存行；已有行的编辑内容尽量保留。
  - 每行可编辑电桩编号、类型和额定功率；类型切换可以提供默认功率建议，但不能覆盖管理员已经手动修改的功率。
  - 提交前校验所有必填字段、功率范围、编号格式及明细内编号唯一性；服务端再次校验与数据库已有编号的唯一性及站点状态约束。
  - 站点和全部初始电桩在同一事务中提交；任一明细非法或写入失败时整体回滚，不留下半成品站点或电桩。
  - 创建成功后返回真实站点 ID 和完整电桩明细，管理列表立即可见并自动展开所属电桩；站点电桩数量、可用数量和在线率从实际关联数据聚合。
  - 单独新增电桩的既有流程继续可用；新建电桩状态固定为 `IDLE`，状态操作仍在设备管理场景中完成。
  - 站点列表创建成功后立即显示真实站点 ID；站点总桩数、可用数和在线率按实际关联电桩聚合，不使用表单数量的临时值。
  - 更新服务端测试、`server/服务端网络接口.md`（如涉及管理员端契约）及必要的总体契约说明；客户端 TCP 接口和数据库迁移保持不变。
- 讨论结论：不再仅依赖服务端自动交替生成快慢充和固定功率作为最终数据；默认值只用于帮助快速填表，管理员可以在提交前逐桩修改。站点创建和初始电桩创建保持原子性；数量为 0 明确表示创建空站点，之后再从电桩管理新增。本项已按该方案实现。
- 依赖与风险：需要同步修改 SQLite 与内存两套 Repository 的批量创建接口和回滚测试；需要确定桩编号格式校验规则及功率上下限；若保留旧 `pileCount` 调用方，需明确兼容策略，避免新旧入参产生歧义。

### 实现记录

- 开始实现：2026-09-04
- 完成实现：2026-09-04
- 代码提交或 PR：待填写
- 验证结果：站点创建支持逐桩明细并保持事务原子性；服务端、Repository 和协议测试全部通过。

## IMP-005：区分充电站与展开电桩的列表层级

- 状态：DONE
- 记录日期：2026-09-04
- 提出背景：当前充电站列表使用同一个 `QTreeWidget` 展示站点和展开后的电桩。父子行虽然存在缩进，但字体、行高、背景和信息组织基本相同，电桩行还把类型、功率和累计数据拼接成描述文字，导致主实体与从属设备混在一起，表格层次不清。
- 目标结果：保留站点展开查看所属电桩的交互入口，同时通过字号、字重、行高、背景、缩进和列内容建立清晰的主从层级。展开区域只承担查看设备基本信息和进入设备管理页的职责，不在站点页面重复承载完整设备详情与操作。
- 影响范围：服务端同进程管理员 UI 的充电站 `QTreeWidget` 展示、站点与电桩行样式、列内容及行级操作布局；不修改数据库、Repository、ApplicationService 或客户端 TCP 接口。
- 接口/数据影响：无。继续使用现有站点和电桩 DTO；不新增字段、表、消息或状态。
- 验收标准：
  - 站点父行使用较大的常规字体（约 14–16px，不使用大面积粗体）、较高行高（约 44–48px）和清晰的名称、地址、状态层级。
  - 电桩子行使用较小的常规字体（约 12–13px）、较紧凑行高（约 34–38px）、浅色背景和明确左侧缩进；不能与站点行视觉等同。
  - 展开区域的电桩只展示四项基本信息：电桩编号、类型、额定功率和状态；不再显示电桩 ID、累计充电次数、累计充电时长等扩展数据。
  - 四项信息按清晰的子表格列展示，不再将多项数据拼接成“电桩 · xxx”“额定功率 xxx kW”等长描述。
  - 站点专属的区域、地址、电价等列在电桩子行中留空，不重复复制站点字段；站点主行保留可用/总数、在线率和状态等主数据。
  - 站点行不在表格中堆叠详情和更多按钮；详情由双击打开，站点编辑、启停、删除及新增电桩等操作统一由右键菜单提供。展开区域的电桩移除详情按钮及设备操作，完整管理功能统一放在充电桩管理页面。
  - 调整后仍可展开/收起站点、打开站点详情，并保留刷新后的展开状态；不同窗口尺寸下不出现明显重叠或无意义横向滚动。
  - 状态文字、颜色语义和按钮名称继续遵循 [`uidesign.md`](uidesign.md) 与 [`logic.md`](logic.md)，不增加开发阶段说明性小字。
- 讨论结论：优先在现有 `QTreeWidget` 上通过父子行样式和四列基本信息完成层级优化，不立即引入复杂的嵌套表格。站点页面只提供设备摘要，设备详情与操作不在此重复；本条以 2026-09-04 本次修订为准，覆盖此前关于在子行展示累计数据、详情按钮和设备操作的旧方案。本项已按该方案实现。
- 依赖与风险：需要在实际窗口尺寸下检查列宽和子行缩进的平衡；现有树表父子行共用表头，需要通过留空和字段映射保证四项设备信息容易理解。如仍不清晰，可评估站点主表加展开区域内轻量子表，但应保持原有展开语义。

### 实现记录

- 开始实现：2026-09-04
- 完成实现：2026-09-04
- 代码提交或 PR：待填写
- 验证结果：站点与电桩父子行已按层级规范显示；展开子行仅保留编号、类型、功率和状态基本信息。

## IMP-006：从充电站行快速新增所属电桩

- 状态：DONE
- 记录日期：2026-09-04
- 提出背景：当前只能从“充电桩管理”页面打开新增电桩窗口，再手动选择所属站点。管理员已经位于某个充电站的操作菜单时，缺少针对该站点直接新增设备的快捷入口。
- 目标结果：在每个充电站行的“更多”菜单中增加“新增充电桩”，打开表单时自动关联当前站点，让管理员直接填写并创建该站点下的电桩。
- 影响范围：服务端同进程管理员 UI 的充电站操作菜单、新增电桩对话框复用和创建成功后的列表刷新；沿用现有 `AdminFacade`、`ApplicationService` 与 Repository 创建电桩能力。
- 接口/数据影响：无。继续使用现有电桩创建入参 `stationId`、`pileCode`、`pileType` 和 `ratedPowerKw`，不修改数据库结构或客户端 TCP 接口。
- 验收标准：
  - 启用站点的“更多”菜单包含“新增充电桩”；停用站点的该菜单项不可用。
  - 从站点行进入时，所属站点自动设为当前站点并锁定，管理员无需也不能误选其他站点。
  - 表单可填写电桩编号、类型和额定功率；初始状态由服务端固定为 `IDLE`，不提供手动状态选择。
  - 提交时继续校验站点存在且启用、电桩编号非空并全局唯一、类型合法及功率有效。
  - 创建成功后刷新相关数据并自动展开原充电站，使新增电桩立即可见；失败时显示明确业务错误且不产生半成品数据。
  - 充电站页面的“更多”文字和菜单指示器在加入该菜单项后保持完整可见；最终统一右键菜单后不再保留拥挤的行内操作按钮。
  - “充电桩管理”页面原有的通用新增入口继续保留，两种入口复用同一套表单和校验逻辑，避免行为不一致。
- 讨论结论：站点行入口服务于“给当前站点加桩”的上下文操作，通用入口服务于先选站点再新增；两者都调用现有服务端创建能力，不新增另一套业务接口。本项已按该方案实现；后续操作入口从行内“更多”按钮统一迁移到右键菜单，最终规则见 `IMP-009`。
- 依赖与风险：实现时应避免复制两套新增对话框代码；刷新后需要保留站点列表当前搜索、筛选和展开状态。

### 实现记录

- 开始实现：2026-09-04
- 完成实现：2026-09-04
- 代码提交或 PR：待填写
- 验证结果：站点行“更多”菜单已增加上下文新增电桩入口，并复用通用表单和校验。

## IMP-007：修复充电站“更多”按钮文字和菜单箭头遮挡

- 状态：DONE
- 记录日期：2026-09-04
- 提出背景：充电站操作列当前固定为 128px，同时容纳“详情”和带菜单箭头的“更多”按钮。按钮宽度、布局间距和水平内边距没有计入菜单指示器占用的空间，导致“更多”文字或箭头被遮挡，影响识别和点击体验。
- 目标结果：重新调整站点行操作区域和按钮尺寸，使“详情”“更多”及下拉箭头在正常字体、系统字体缩放和高 DPI 环境下完整显示，同时保持操作列紧凑、不显笨重。
- 影响范围：服务端同进程管理员 UI 的充电站操作列、按钮样式和布局；可与 `IMP-005` 的父子列表层级调整统一实施。
- 接口/数据影响：无；纯 UI 布局和样式调整。
- 验收标准：
  - “详情”和“更多”按钮文字完整显示，“更多”的菜单箭头不与文字重叠、不被裁切。
  - 操作列宽度按按钮实际 `sizeHint`、菜单指示器、布局间距和边距计算或设置合理的最小宽度，不再依赖刚好容纳当前字体的硬编码值。
  - 按钮水平内边距保持紧凑，操作列建议控制在约 136–144px，但应以实际完整显示为准，不能为追求固定数值再次截断。
  - 在常用窗口尺寸、系统字体缩放和高 DPI 下检查显示；按钮不重叠，表格其他关键列不因此产生无意义的大面积压缩。
  - 加入 `IMP-006` 的“新增充电桩”菜单项后，菜单能够完整弹出，且不改变“更多”按钮文字区域。
  - 电桩子行的操作控件保持比站点父行更紧凑，并与 `IMP-005` 的层级规范一致。
- 讨论结论：本项记录了早期行内“更多”按钮的宽度和菜单箭头遮挡修复，问题应从尺寸策略解决，而不是缩短文字或隐藏箭头。随后负责人确认统一采用右键菜单并移除行内操作列，最终规则见 `IMP-009`；本项保留为历史修复记录。
- 依赖与风险：Qt 不同平台样式对菜单箭头和按钮 `sizeHint` 的计算可能不同，需在实际 Qt 运行环境中进行视觉冒烟验证。

### 实现记录

- 开始实现：2026-09-04
- 完成实现：2026-09-04
- 代码提交或 PR：待填写
- 验证结果：早期行内按钮尺寸修复已通过编译验证；随后行内操作列按 `IMP-009` 统一移除，最终以右键菜单承载操作。

## IMP-008：双击展开电桩跳转并定位到充电桩管理页

- 状态：DONE
- 记录日期：2026-09-04
- 提出背景：充电站展开区域只应提供所属电桩的基本信息，但管理员仍需要从当前站点快速进入某台设备的完整管理位置。若仅移除详情和操作而没有明确入口，管理员还需手动切换页面并重新查找电桩。
- 目标结果：管理员双击充电站下展开的电桩行后，系统跳转到“充电桩管理”页面，自动显示、选中并滚动定位到刚才双击的电桩，便于继续查看详情或执行设备操作。
- 影响范围：服务端同进程管理员 UI 的树表双击事件、侧栏页面导航、充电桩搜索/筛选状态、列表选中与滚动定位；不修改服务端业务接口、Repository、数据库或客户端 TCP 协议。
- 接口/数据影响：无。使用电桩行已有的 `pileId` 和 `stationId` 完成页面内导航与定位。
- 验收标准：
  - 双击站点父行仍按原有逻辑处理，不误触发电桩跳转；只有双击电桩子行才跳转。
  - 跳转后侧栏和页面标题正确切换到“充电桩管理”。
  - 系统自动清除会隐藏目标电桩的关键词和状态筛选，并将所属站点筛选设置为目标电桩的站点，确保目标行可见。
  - 列表刷新后按稳定的 `pileId` 查找目标，而不是依赖刷新前的行号；自动选中目标行并滚动到可视区域。
  - 定位成功后只高亮目标行，不自动打开详情、不自动执行任何设备操作。
  - 若目标电桩在跳转前已被删除或读取失败，仍进入充电桩管理页并显示明确提示，不选中错误行。
  - 完成跳转后，管理员可在充电桩管理页使用既有详情、开关机、重启、标记故障和删除功能。
- 讨论结论：站点展开区是设备摘要入口，双击是进入完整设备管理的快捷方式；不在摘要区保留“详情”按钮或设备操作。本项已与修订后的 `IMP-005` 配套实现，并以本次记录覆盖此前对子行操作入口的旧设想。
- 依赖与风险：刷新列表时筛选控件会触发多个信号，需要避免重复刷新导致定位丢失；系统字体与鼠标操作之外，还应考虑后续为键盘用户提供等价入口，但不在本项强制增加新按钮。

### 实现记录

- 开始实现：2026-09-04
- 完成实现：2026-09-04
- 代码提交或 PR：待填写
- 验证结果：双击电桩子行可跳转充电桩管理页，按站点筛选并按 pileId 选中和滚动定位。

## IMP-009：统一列表单击、双击详情与右键操作交互

- 状态：DONE
- 记录日期：2026-09-04
- 提出背景：当前管理端部分页面同时存在按钮、双击和展开等不同入口，操作列容易拥挤；站点、电桩、用户和订单的行级交互规则也不完全一致。需要建立桌面端管理员容易理解、可发现且可扩展的统一交互模型。
- 目标结果：统一各管理列表的单击、双击和右键行为，减少操作列视觉负担，同时保留详情入口和完整业务操作。
- 影响范围：服务端同进程管理员 UI 的充电站 `QTreeWidget`、充电桩/用户/订单 `QTableWidget`，页面导航、行选中、详情展示及上下文菜单；不修改数据库、Repository、ApplicationService 或客户端 TCP 接口。
- 接口/数据影响：无。继续使用现有 DTO、AdminFacade 能力和页面内导航状态。
- 交互规则：
  - 充电站主行：单击展开/收起所属电桩；双击打开站点详情；右键弹出更多菜单。
  - 充电站展开下的电桩：单击选中；双击跳转到充电桩管理页并高亮、滚动定位目标；右键可弹出电桩相关菜单（至少包含前往电桩管理、查看详情或可用操作，具体以页面职责为准）。
  - 充电桩管理行：单击选中；双击打开电桩详情；右键弹出更多菜单。
  - 用户管理行：单击选中；双击打开用户详情；右键弹出更多菜单。
  - 订单管理行：单击选中；双击打开订单详情；右键弹出更多菜单。
  - 右键点击有效行时先选中该行，确保菜单操作作用于当前行；空白区域右键不弹出业务菜单。
- 右键菜单内容：
  - 充电站：查看详情、管理站内充电桩、编辑站点、新增充电桩、启用/停用、删除（依据站点状态启用或禁用）。
  - 充电桩：查看详情、修改编号/类型/功率、上线、下线、重启、标记故障、删除；依据当前状态启用或禁用。
  - 用户：查看详情、冻结/解冻；存在进行中订单时冻结操作禁用并显示原因。当前版本不在右键菜单中伪造未提供的昵称编辑能力。
  - 订单：查看详情、复制订单号；导出当前订单或相关记录暂未提供，不在菜单中伪造该能力。
- 验收标准：
  - 站点主行单击只负责展开/收起，双击只打开详情，不因双击产生两次不可预期的展开切换。
  - 展开电桩双击仍按 `IMP-008` 跳转到充电桩管理并按稳定 `pileId` 高亮定位；不自动弹出详情。
  - 充电桩、用户和订单列表双击分别打开对应详情；右键菜单可访问该页面已有的修改或状态操作。
  - 移除或不再依赖站点、电桩、用户、订单操作列中的“更多”按钮，避免按钮遮挡和拥挤；详情不再必须依赖操作列按钮。
  - 右键菜单不会作用于上一行或空白区域；菜单项状态与现有业务约束一致，不提供无法执行的假操作。
  - 详情关闭后返回原列表位置，并保留搜索、筛选、排序、站点展开和目标行选中状态。
  - 鼠标和键盘操作均可完成基本导航；界面不增加开发阶段说明性小字。
- 讨论结论：负责人确认后采用“单击选择/展开、双击详情或上下文跳转、右键更多操作”的统一桌面端模式。站点展开区的电桩双击跳转规则优先于通用双击详情规则；充电桩管理页中的电桩双击才打开电桩详情。右键菜单替代拥挤的“更多”按钮，详情与高频主路径不依赖隐藏菜单；本项已实现。
- 依赖与风险：Qt 的 `itemPressed`、`itemDoubleClicked` 和自定义上下文菜单信号需要协调，避免单击展开与双击详情冲突；菜单中的“修改站点/修改电桩/导出订单”等能力若服务端尚未提供，必须先置灰或从菜单隐藏，不能伪造功能。

### 实现记录

- 开始实现：2026-09-04
- 完成实现：2026-09-04
- 代码提交或 PR：待填写
- 验证结果：站点、电桩、用户、订单列表已支持单击选择/展开、双击详情或跳转、右键上下文菜单；移除列表操作按钮；编译与自动化测试通过。

## IMP-010：将详情改为居中的结构化对话框

- 状态：DONE
- 记录日期：2026-09-04
- 提出背景：当前站点、电桩、用户和订单详情通过右侧 `QDockWidget` 中的 `QTextBrowser` 以长段落文字展示，字段层级弱、扫描效率低，也与新增表单的居中窗口体验不一致。
- 目标结果：双击列表行后打开居中的详情对话框，使用分组、字段标签和只读控件排列具体信息；详情窗口与新增窗口保持统一的居中、留白和按钮风格，不再依赖右侧停靠栏承载主要详情。
- 影响范围：服务端同进程管理员 UI 的详情展示组件和站点、电桩、用户、订单四类详情窗口；不修改数据库、Repository、ApplicationService、客户端 TCP 接口或 DTO。
- 接口/数据影响：无。继续使用现有 `listStations`、`listPiles`、`listUsers`、`listOrders` 返回的数据；不新增字段或消息。
- 详情布局：
  - 充电站：基本信息（名称、状态、区域、地址、电价、经纬度）+ 运营概览（可用/总桩数、在线率）+ 所属电桩小表格（编号、类型、功率、状态）。
  - 充电桩：基本信息（编号、所属站点、类型、额定功率、状态）+ 运行统计（累计充电次数、累计充电时长）。
  - 用户：账户信息（手机号、昵称、状态、余额、注册时间）+ 业务概览（订单总数、累计消费）。
  - 订单：订单信息（订单号、用户、站点、电桩、模式、状态）+ 时间信息（创建、预约、开始、结束、支付）+ 计费信息（时长、电量、价格快照、最终金额）。
- 交互规则：
  - 详情窗口为只读，不在详情中直接修改业务数据；修改、状态变更和危险操作通过右键“更多”菜单进入。
  - 详情窗口不再提供底部关闭按钮；点击窗口外区域关闭，点击窗口内部不会误关闭；关闭后回到原列表并保留搜索、筛选、展开和选中状态。
  - 站点详情中的所属电桩只展示基本信息，不重复设备操作；设备完整管理仍在充电桩管理页面。
  - 站点展开电桩双击继续遵循 `IMP-008`：跳转到充电桩管理并高亮定位，不打开详情窗口；充电桩管理列表双击才打开电桩详情。
- 验收标准：
  - 四类详情均显示为居中的模态或非模态 `QDialog`，窗口大小适合内容，不能再使用右侧停靠栏作为主要详情载体。
  - 字段使用清晰的标签—值两列或网格布局；金额、时间、状态、单位格式与现有契约一致；长地址等文本可换行或合理省略。
  - 站点详情的电桩表格列清晰、轻量，不出现累计数据、详情按钮或设备操作按钮。
  - 用户和订单详情完整重复列表中已有的关键信息，再补充其余字段，避免上下文丢失。
  - 详情加载失败或目标记录不存在时显示明确错误，不打开空白窗口。
  - 双击、右键菜单和关闭窗口不会破坏原列表的筛选、展开和选中状态。
  - 移除或停用旧 `QDockWidget` 详情实现及其相关状态，避免新旧两套详情同时出现。
- 讨论结论：负责人确认后，详情采用与新增窗口一致的居中结构化 UI；详情只读，业务修改统一从右键操作菜单进入。最终关闭方式为点击详情窗口外的区域，移除底部“关闭”按钮；本项已实现。
- 依赖与风险：需要协调 `IMP-009` 的双击和右键事件，避免双击触发两次详情；站点详情包含子表格时需控制窗口尺寸和高 DPI 下的可读性。

### 实现记录

- 开始实现：2026-09-04
- 完成实现：2026-09-04
- 代码提交或 PR：待填写
- 验证结果：详情统一改为居中模态只读对话框，按字段表单布局展示；窗口根据内容紧凑自适应，内容较多时滚动，点击外部关闭且无关闭按钮；编译与自动化测试通过。

## IMP-011：精简管理列表字段，扩展详情承载信息

- 状态：DONE
- 记录日期：2026-09-04
- 提出背景：当前站点、电桩、用户和订单列表展示了较多低频字段，表格密度高、扫描重点不突出。部分字段管理员只有在查看详情时才需要，不应长期占据列表列宽。
- 目标结果：按管理员日常判断任务重新定义四个管理列表的最小核心字段；低频和扩展信息统一移到 `IMP-010` 的结构化详情窗口，列表只保留快速识别、筛选和操作所需内容。
- 影响范围：服务端同进程管理员 UI 四个列表的列定义、刷新填充、列宽和详情数据映射；与 `IMP-009` 的双击/右键交互、`IMP-010` 的居中详情窗口配套；不修改数据库、Repository、ApplicationService、DTO 或客户端 TCP 接口。
- 接口/数据影响：无。列表和详情继续使用现有 DTO 字段；只是调整字段展示位置，不删除数据库或接口字段。
- 列表字段规范：
  - 充电站管理：`ID`、站点名称、区域、可用/总数、在线率、电价、状态；`ID` 固定为第一列。地址、经纬度、创建时间和所属电桩扩展资料只在详情中展示。
  - 充电桩管理：`ID`、电桩编号、所属站点、类型、状态。额定功率、累计充电次数和累计充电时长移到电桩详情。
  - 用户管理：`ID`、手机号、昵称、余额、状态。注册时间、订单统计、累计消费移到用户详情。
  - 订单管理：订单 `ID`、订单号、用户手机号、站点、电桩、模式、状态、金额。时长、电量、创建时间、预约/开始/结束/支付时间和价格快照移到订单详情。
- 交互与详情要求：
  - 列表字段精简后，详情必须重复该行的核心字段，再补充移出的低频字段，避免管理员失去上下文。
  - 站点展开下的电桩继续只展示编号、类型、功率和状态摘要，不受本项“电桩管理列表”列定义影响；双击跳转规则遵循 `IMP-008/009`。
  - 不再依赖操作列承载详情和更多按钮；详情由双击打开，编辑、状态和删除等操作由右键菜单提供，不通过增加列恢复被移出的字段。
  - 用户和订单列表中的“用户”显示手机号，不显示用户 ID；订单详情仍保留用户 ID 作为辅助关联信息。
- 验收标准：
  - 四个列表的可见列与上述规范完全一致，低频字段不再占用列表列位。
  - 充电站列表的 ID 位于第一列；其他页面 ID 也按规范保持稳定位置。
  - 用户列表不显示注册时间；电桩列表不显示功率和累计统计；订单列表不显示时长、电量和创建时间。
  - 订单列表用户列显示手机号；当手机号无法映射时使用明确的业务占位文本，不显示错误的内部 ID 代替。
  - 双击打开的结构化详情仍包含被移出的全部字段，金额、时间、单位和状态格式正确。
  - 搜索、筛选、排序、行选中、右键菜单和详情打开不因列数减少而失效；不同窗口尺寸下不出现不必要的横向滚动。
  - 不删除 DTO、数据库字段或服务端返回值；后续导出等功能仍可使用完整数据。
- 讨论结论：列表遵循“最少但足够判断”的原则，详情承担完整信息；本项只改变 UI 展示位置和列顺序，不改变业务数据。与 `IMP-009`、`IMP-010` 一起实施后，字段没有从 DTO、数据库或服务端返回值中删除，详情保留完整上下文；本项已实现。
- 依赖与风险：订单用户手机号需要从现有用户列表按 `userId` 建立内存映射；数据量增大后再考虑服务端联表或专用详情接口。列精简后需在实际窗口宽度和高 DPI 环境检查可读性。

### 实现记录

- 开始实现：2026-09-04
- 完成实现：2026-09-04
- 代码提交或 PR：待填写
- 验证结果：四个管理列表已按核心字段精简，低频字段保留在详情中；站点 ID 前置、订单用户列显示手机号；编译与自动化测试通过。

## IMP-012：增加搜索字段选择、独立筛选和站点批量展开

- 状态：DONE
- 记录日期：2026-09-04
- 提出背景：当前各页面只有一个无字段提示的关键词输入框，管理员不清楚关键词匹配的对象；区域、状态、站点等筛选控件与搜索区混在一起，列表关系不够直观；充电站树表缺少一次性展开或收起全部站点的操作。
- 目标结果：在搜索框前增加搜索字段选择，将筛选控件视觉上归入列表表头邻近区域，并为充电站列表提供“全部展开/全部收起”，同时保持搜索和筛选是两个独立动作。
- 影响范围：服务端同进程管理员 UI 的充电站、充电桩、用户、订单四个列表工具栏/表头邻近区域、关键词过滤逻辑和站点树展开控制；必要时调整进程内列表过滤参数，但不修改数据库、DTO 或客户端 TCP 接口。
- 接口/数据影响：无。继续使用现有列表返回数据和本地过滤；搜索字段选择只改变 UI 端匹配范围，不新增网络消息或数据库字段。
- 搜索字段：
  - 充电站：站点名称、地址、全部。
  - 充电桩：电桩编号、所属站点、全部。
  - 用户：手机号、昵称、全部。
  - 订单：订单号、用户手机号、电桩编号、全部。
  - 搜索字段切换只改变匹配范围；关键词输入采用即时刷新，不再设置独立的“搜索”按钮或回车提交步骤。
- 筛选布局与行为：
  - 区域、状态、站点、模式等离散筛选控件从关键词输入行中视觉分离，放在列表表头上方或紧邻表头的筛选行；不直接嵌入原生表头单元格，避免破坏 Qt 表头滚动和高 DPI 布局。
  - 筛选弹窗内可多选并点击确认后生效；筛选与关键词条件按 AND 关系叠加。
  - “重置”清空关键词、恢复搜索字段默认值并清除所有筛选；充电站页面同时收起全部展开项，不改变页面主操作按钮位置。
- 充电站批量展开：
  - “全部展开”展开当前筛选/搜索结果中所有可见充电站；“全部收起”收起所有站点。
  - 操作只影响当前站点列表视图，不改变数据库和业务状态；刷新后继续保留展开状态，新建站点成功后继续自动展开新站点。
  - 单击站点主行仍负责单个展开/收起，双击主行打开详情；展开电桩双击继续按 `IMP-008/009` 跳转到充电桩管理并定位。
- 验收标准：
  - 四个页面均在关键词输入框前显示搜索字段下拉框，字段选项与上述规范一致，当前字段在视觉上清晰可见。
  - 关键词输入会即时刷新匹配结果；切换搜索字段只改变匹配范围，筛选条件不被清除。
  - 筛选控件位于搜索栏下方并靠近列表的独立区域，视觉上与列表关联但不挤压列标题；筛选弹窗点击确认后一次刷新结果。
  - 充电站列表具备“全部展开”和“全部收起”按钮，按钮只作用于当前可见站点，执行后展开状态正确。
  - 搜索字段、关键词和筛选条件组合结果正确；重置后恢复完整列表和默认控件状态。
  - 与 `IMP-009`、`IMP-010`、`IMP-011` 的单击/双击/右键、详情窗口和精简列定义兼容；不重新引入被移出列表的低频字段或开发说明文案。
  - 窗口缩放和高 DPI 下控件不重叠，列表表头、筛选区和主操作按钮保持可读。
- 讨论结论：负责人确认后采用“字段选择 + 关键词即时搜索 + 独立筛选确认生效”的模型；筛选控件位于搜索栏附近并靠近列表，不嵌入原生表头；筛选弹窗支持多选、全选和一次确认，移除取消按钮；充电站提供合并的批量展开/收起按钮，重置同时清空搜索、筛选并收起站点。本项已实现。
- 依赖与风险：订单用户手机号需要沿用现有内存映射；如果未来改为服务端分页，搜索字段和筛选参数需保持同样语义。批量展开需在刷新后正确恢复状态，避免信号触发重复刷新。

### 实现记录

- 开始实现：2026-09-04
- 完成实现：2026-09-04
- 代码提交或 PR：待填写
  - 验证结果：四页增加搜索字段选择并采用即时关键词搜索；筛选弹窗支持多选、全选和确认应用；站点增加合并的全部展开/收起；编译与自动化测试通过。

## IMP-013：统一页面浏览历史与当前页刷新

- 状态：DONE
- 记录日期：2026-09-04
- 提出背景：管理员需要在不同管理页面之间往返查看同一业务上下文，同时能够重新读取最新数据；刷新不能被误解为撤销数据库操作。
- 目标结果：在顶部后退与前进按钮之间加入刷新按钮，形成统一的页面浏览工具组。后退/前进恢复页面及其搜索、筛选、展开、定位和日期配置；刷新仅重置当前页面配置并重新加载当前页数据，不修改数据库、不刷新其他页面、不改变前进后退历史。
- 影响范围：服务端管理员端 Qt Widgets 顶部导航工具栏和六个管理页面的视图状态。
- 接口/数据影响：无。继续使用现有列表和统计接口，不新增数据库字段或客户端 TCP 接口。
- 验收标准：刷新按钮使用常见刷新图标并位于后退和前进之间；六个页面均恢复各自默认视图并只重新取数；刷新前后的前进/后退栈保持可用；数据操作不进入浏览历史；窗口缩放下按钮保持一致布局。
- 讨论结论：采用浏览器式语义：浏览历史只记录页面状态，不记录数据库写操作；刷新是当前页面视图重置与重新取数，之后跳转页面时按正常规则处理前进栈。
- 依赖与风险：无。

### 实现记录

- 开始实现：2026-09-04
- 完成实现：2026-09-04
- 代码提交或 PR：待填写
- 验证结果：编译成功；charging_server_tests、charging_repository_tests、charging_protocol_tests 全部通过；服务端已重启。

## IMP-014：优化运营监控状态饼图悬停信息

- 状态：DONE
- 记录日期：2026-09-04
- 提出背景：运营监控状态饼图右侧常驻信息和中心总数占用视觉空间，管理员查看某个状态时仍需要额外对照。
- 目标结果：仅保留左侧状态环形图，移除右侧常驻图例/数量和中心文字；鼠标悬停在颜色区域时显示状态、数量/电桩总数和占比；继续支持点击颜色区域跳转充电桩管理并按状态筛选。
- 影响范围：服务端管理员端 Qt Widgets 的运营监控状态图组件。
- 接口/数据影响：无。沿用已有状态统计数据和点击跳转信号。
- 验收标准：图表右侧不再绘制信息列表，中心无永久文字；悬停每个非空扇区能看到正确的状态、数量/总数和百分比；离开区域提示消失；点击扇区仍能完成状态筛选跳转。
- 讨论结论：本条记录的是一次中间迭代：曾暂时移除右侧图例和中心文字，仅以 Qt 原生工具提示承载扇区信息；随后由 `IMP-015` 恢复轻量图例并扩展为图例悬停/点击。因此当前最终 UI 以 `IMP-015` 为准。
- 依赖与风险：无。

### 实现记录

- 开始实现：2026-09-04
- 完成实现：2026-09-04
- 代码提交或 PR：待填写
- 验证结果：中间方案已通过编译和测试；最终图例、悬停和点击行为由 `IMP-015` 重新验证。

## IMP-015：状态图例悬停提示与停用站点联动下线

- 状态：DONE
- 记录日期：2026-09-04
- 提出背景：状态饼图在小数量场景下不易准确点击；充电站停用时需要保证站内设备不会继续接收新的预约或充电任务。
- 目标结果：饼图右侧恢复仅包含颜色和状态名称的轻量图例，图例与扇区共用悬停详情和点击跳转；停用充电站前检查站内电桩，存在预约中或充电中的电桩则拒绝停用；停用成功后将空闲在线电桩统一置为离线，故障和已离线状态保持不变；启用充电站不自动开机电桩。
- 影响范围：服务端管理员 Qt Widgets 状态图组件、ApplicationService 管理员站点状态业务规则及内存/SQLite 共享仓储调用路径。
- 接口/数据影响：不新增字段和接口；复用现有站点/电桩状态及 `IllegalOrderState` 错误码，新增业务消息 `STATION_HAS_ACTIVE_PILES` 供管理员端展示。
- 验收标准：图例只显示颜色和状态；鼠标悬停扇区或图例显示状态、数量/总数、占比；点击扇区或图例均按状态跳转筛选；有预约/充电电桩时停用失败且站点状态不变；无活动电桩时停用成功并仅将空闲电桩改为离线；故障电桩不被覆盖；重新启用不自动恢复离线电桩。
- 讨论结论：状态图保留环形和左侧布局，图例作为可操作的辅助命中区域；停用规则在业务服务层执行，避免仅依赖 UI 校验造成状态绕过。
- 依赖与风险：无。

### 实现记录

- 开始实现：2026-09-04
- 完成实现：2026-09-04
- 代码提交或 PR：待填写
- 验证结果：编译成功；charging_server_tests、charging_repository_tests、charging_protocol_tests 全部通过；服务端已重启。

## IMP-016：运营监控与营收统计页面拆分

- 状态：DONE
- 记录日期：2026-09-04
- 提出背景：原“数据概览”同时承载设备状态和营收趋势，信息目标不清晰；负责人明确要求把设备运行判断和营收分析拆成两个导航页面。
- 目标结果：左侧导航将“数据概览”拆为“运营监控”和“营收统计”，保持原有导航风格。运营监控聚焦电桩状态及站点统计；营收统计聚焦收入指标和趋势。
- 影响范围：服务端同进程 Qt 管理员端导航、页面构建和刷新分发；ApplicationService 继续提供现有统计数据；不涉及客户端 TCP 接口。
- 接口/数据影响：无新增接口、表或字段。运营监控使用站点/电桩列表聚合状态；营收统计继续使用 `getDashboard` 的日期范围数据。
- 验收标准：运营监控左上显示状态环形图，右上留空，下方表格占满宽度；营收统计保留 7 日、30 日和自定义日期范围及营收趋势，移除与收入无关的状态卡。
- 讨论结论：页面拆分是面向运营任务的信息架构调整，不把数据库、TCP、Repository 或开发模式暴露给管理员；本项已实现。
- 依赖与风险：无。

### 实现记录

- 开始实现：2026-09-04
- 完成实现：2026-09-04
- 代码提交或 PR：待填写
- 验证结果：导航、两页布局和数据刷新均通过编译及启动验证。

## IMP-017：统一管理页面视觉层级与轻量化排版

- 状态：DONE
- 记录日期：2026-09-04
- 提出背景：充电站主表、展开子表以及充电桩、用户、订单三个管理表的字体和行高多次调整后不一致；导航、平台标题和页面标题偏小，部分布局显得笨重。
- 目标结果：在不改变侧边栏宽度的前提下，统一四个管理列表的正文、表头、行高和列间距，并明确充电站主行与子电桩行的层级；同步放大导航字体/图标、左上平台标题、页面标题和顶部工具控件，左下只保留退出登录。
- 影响范围：服务端 Qt Widgets 全局 QSS、导航栏、页面标题、四个管理列表、站点树表代理和顶部后退/刷新/前进控件。
- 接口/数据影响：无。纯 UI 样式与排版，不改变业务数据和接口。
- 验收标准：四个管理主表行高统一为 48px、正文使用常规字重；充电站子行约 40px 且字号略小；主表表头字号和页面工具栏控件保持一致；站点表头列不可拖动；不出现开发阶段说明性小字或管理员账号展示。
- 讨论结论：遵循 `uidesign.md` 的舒适字号、有限字重、中性背景和轻量分隔线原则；平台标题放大，左下移除“系统管理员 admin”，仅保留“退出登录”；本项已实现。
- 依赖与风险：Qt 不同平台字体度量可能略有差异，需在目标环境继续做视觉验收。

### 实现记录

- 开始实现：2026-09-04
- 完成实现：2026-09-04
- 代码提交或 PR：待填写
- 验证结果：四个列表、导航和标题样式已统一，编译通过。

## IMP-018：搜索、筛选、重置与站点树控制最终规则

- 状态：DONE
- 记录日期：2026-09-04
- 提出背景：搜索字段不明确、筛选曾与搜索共用提交、无法多选，且站点树批量展开和重置行为不稳定，导致筛选结果与页面状态互相干扰。
- 目标结果：四个列表均提供搜索字段选择和即时关键词搜索；筛选使用独立多选弹窗，点击确认后按所选条件过滤；搜索与筛选可分别使用，也可按 AND 关系组合；重置一次清空当前页面全部条件。
- 影响范围：服务端 Qt 管理员端四个列表工具栏、QTreeWidget/QTableWidget 本地过滤、站点展开状态和电桩跳转定位。
- 接口/数据影响：无。继续读取现有 DTO，在 UI 侧过滤，不新增网络消息或数据库字段。
- 最终规则：站点搜索名称/地址；电桩搜索编号/所属站点；用户搜索手机号/昵称；订单搜索订单号/用户手机号/电桩编号；均支持“全部”。区域、状态、站点和模式筛选支持多选、全选和确认，取消按钮移除。筛选按钮位于搜索栏附近；站点的“全部展开/全部收起”按当前展开情况切换并同步文字；重置同时恢复默认字段、清空关键词和筛选，站点页额外全部收起。
- 验收标准：关键词输入立即刷新；筛选确认后一次应用完整选择而非每次只能选一项；筛选与关键词组合结果正确；筛选条件不会因切页、刷新或电桩定位被错误丢失；充电站表头禁止拖动。
- 讨论结论：以本条为最终交互规则，覆盖此前“点击搜索/回车提交”和“单选筛选”的中间设计；本项已实现。
- 依赖与风险：未来服务端分页时需保持相同字段语义和 AND 组合语义。

### 实现记录

- 开始实现：2026-09-04
- 完成实现：2026-09-04
- 代码提交或 PR：待填写
- 验证结果：四页搜索和筛选已修复并通过手动场景校对、编译和测试。

## IMP-019：站点与电桩上下文操作及安全状态规则

- 状态：DONE
- 记录日期：2026-09-04
- 提出背景：管理员需要在站点上下文中管理所属设备，也需要编辑电桩基本资料和控制设备状态；站点停用还必须避免影响预约中或充电中的业务。
- 目标结果：站点右键提供管理站内电桩、编辑站点、新增电桩、启用/停用和删除；电桩右键提供修改信息、上线、下线、重启、标记故障和删除。编辑弹窗允许修改站点资料或电桩编号、类型、额定功率；新增电桩必须关联启用站点且编号唯一。
- 影响范围：Qt 管理端上下文菜单和表单；AdminFacade、ApplicationService、SQLite/内存 Repository 的更新、删除和状态调用；服务端测试。
- 接口/数据影响：新增管理员进程内 facade/service 方法 `updateStation`、`setStationStatus`、`updatePile` 及对应 Repository 更新能力；不新增客户端 TCP 消息、数据库表或字段。停用失败使用 `STATION_HAS_ACTIVE_PILES` 消息。
- 安全状态规则：预约中或充电中的电桩不可编辑、删除或停用所属站点；停用站点时空闲在线桩改为离线，故障桩和原离线桩保持原状态；重新启用不自动开机；故障状态不能通过普通上线或重启绕过。
- 验收标准：业务约束由 ApplicationService 执行而非只依赖 UI 置灰；失败不产生半成品状态；删除保留有订单数据的站点/电桩；成功后受影响列表刷新并立即反映聚合数量和状态。
- 讨论结论：站点和电桩从属关系由现有 `charging_stations` 与 `charging_piles` 维护；新增站点初始电桩数量可为 0，数量大于 0 时逐桩明细可编辑并原子创建；本项已实现。
- 依赖与风险：真实设备控制协议和故障报修闭环仍未接入，当前操作更新的是管理端业务状态。

### 实现记录

- 开始实现：2026-09-04
- 完成实现：2026-09-04
- 代码提交或 PR：待填写
- 验证结果：站点/电桩编辑、删除、状态安全规则测试通过；SQLite 与内存仓储编译通过。

## IMP-020：详情窗口最终交互与页面上下文保留

- 状态：DONE
- 记录日期：2026-09-04
- 提出背景：详情先后经历右侧停靠栏、居中窗口、关闭按钮和外部点击关闭等调整，最终需要统一成紧凑、自适应且不打断列表上下文的体验。
- 目标结果：站点、电桩、用户、订单双击后均打开居中的结构化只读窗口；字段按标签和值排列，站点详情包含轻量所属电桩表格；内容少时紧凑，内容多时滚动；点击窗口外关闭，窗口内部点击不关闭，不提供关闭按钮。
- 影响范围：Qt Widgets 详情组件、四个管理页面双击事件和站点子桩跳转；不改变 DTO、数据库或客户端接口。
- 接口/数据影响：无。继续使用现有列表 DTO，并在 UI 层组织展示。
- 验收标准：详情不再显示为右侧 QDockWidget；窗口尺寸受屏幕约束并按内容自适应；详情重复列表核心字段并补充低频字段；关闭后原页面搜索、筛选、站点展开、选中项和电桩定位状态保持不变。
- 讨论结论：最终采用窗口失活/点击外部关闭语义，移除关闭按钮；站点子电桩双击仍跳转充电桩管理并高亮，不弹出详情；本项已实现。
- 依赖与风险：无。

### 实现记录

- 开始实现：2026-09-04
- 完成实现：2026-09-04
- 代码提交或 PR：待填写
- 验证结果：四类详情窗口完成紧凑自适应和外部点击关闭校对，编译通过。

## IMP-021：页面浏览历史的跨页定位补充

- 状态：DONE
- 记录日期：2026-09-04
- 提出背景：在 `IMP-013` 已确定浏览器式后退/刷新/前进语义后，负责人进一步要求站点子桩和运营监控状态图跳转时保留完整来源页面，并在返回时恢复原配置。
- 目标结果：补充跨页跳转的状态快照规则：顶部后退、刷新、前进组成一个贯通的浏览工具组；历史保存页面、搜索字段、关键词、筛选、展开、选中、电桩定位和营收日期范围；刷新只重置当前页视图并重新取数；数据写操作不进入历史。
- 影响范围：Qt 管理员端导航、页面状态快照、站点/电桩跨页定位、登录登出生命周期。
- 接口/数据影响：无。只保存 UI 状态，不新增数据库、接口或撤销日志。
- 验收标准：后退/前进可恢复完整页面配置；运营监控状态图点击状态后能定位到电桩筛选；站点子桩双击后能按站点筛选并高亮目标；刷新不刷新其他页、不改变历史栈、不修改数据库；登出清空历史。
- 讨论结论：本项补充 `IMP-013` 的跨页定位细节，采用浏览器式页面历史语义，明确不提供数据撤销；本项已实现。
- 依赖与风险：无。

### 实现记录

- 开始实现：2026-09-04
- 完成实现：2026-09-04
- 代码提交或 PR：待填写
- 验证结果：导航历史、当前页刷新和跨页定位通过编译、测试与启动验收。

## 修改项模板

后续每个修改项使用以下格式追加，编号保持递增，不删除历史记录：

```markdown
## IMP-XXX：简短标题

- 状态：TODO
- 记录日期：YYYY-MM-DD
- 提出背景：说明当前问题或使用场景。
- 目标结果：说明完成后应该达到什么效果。
- 影响范围：服务端模块、数据库、客户端接口或管理员端 UI。
- 接口/数据影响：新增或调整的消息、字段、表、状态和兼容性要求；无则写“无”。
- 验收标准：列出可以判断完成的具体条件。
- 讨论结论：记录已经确认的方案和明确不做的内容。
- 依赖与风险：记录数据库负责人、客户端负责人或其他外部依赖；无则写“无”。

### 实现记录

- 开始实现：YYYY-MM-DD
- 完成实现：YYYY-MM-DD
- 代码提交或 PR：待填写
- 验证结果：待填写
```

## 记录原则

- 讨论中的想法不自动视为开发授权，也不自动写入“已确认”列表；
- 只记录已经明确的目标和约束，不把推测写成需求；
- 涉及跨模块接口、DTO、错误码或数据库结构时，先记录影响，再决定是否需要同步其他模块文档；
- 不在本文件中粘贴密码、token、数据库密钥或其他敏感信息；
- 已完成项保留在本文档中，便于追踪历史，不直接删除。
