# 演示数据目录

`demo.sql` 提供管理员、普通/冻结用户、3 个站点、12 个 Mock 桩，以及已完成/预约/充电中/待支付订单。它保留契约联调使用的 `13800000001`、`PILE-A-01`、`PILE-A-02` 和 `admin / 123456` 场景。

种子使用固定主键和 `INSERT OR IGNORE`，可对同一开发库重复执行而不重复插入。订单日期以首次插入当天为基准，近 7/30 个中国业务日均有多个营收点；再次执行不会刷新已有记录的时间。

该文件只用于新建的开发/演示库，不是生产数据迁移，也不是恢复标准状态的重置脚本。
